check_same_thread=False

seguridad (e.g., validación del redireccionamiento, para "sanearla")


"safe"

"recordarme"

estilo

hacer más rápido --> más lindo

minificar para hacer mas veloz la carga

que los campos sean obligatorios al crear new entries
----
  if ($('#ubicacion').val() == ''){  
    $(document).on("click", "#telef", function() {
      // Restore it
      $("#ubicacion").replaceWith(replaced);
      console.log('restoring')
    });
  };

  ---
form tiene valor de ave por defecto

funcion ubicacion():
    set old value
    si click en form:
        --> guardar old value
        --> empty value
        if enter:
            if value not null:
                --> set new value
                --> send new value
        else if click afuera:
            --> ubicacion()


var replaced;
  $(document).on("click", "#ubicacion", function() {
    // Save what's being replaced
    replaced = $(this).replaceWith('<td><form><input type="text" id="ubicacion"></form></td>');
    $("#ubicacion").focus()
    console.log('replacing');

  if ($('#ubicacion').val() == ''){  
    $(document).on("click", "#telef", function() {
      // Restore it
      $("#ubicacion").replaceWith(replaced);
      console.log('restoring')
    });
  };
